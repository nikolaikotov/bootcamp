<%= #form_with model: @development_plan do |form| %>
  	Title
  <%= form.text_field :title %>
  	DepartmentID
 <%= form.collection_select :department_id, Department.order(:title), :id, :title %>
  <%= form.submit %>
<% end %>

